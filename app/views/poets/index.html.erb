<p id="notice"><%= notice %></p>

<h1>شعراء</h1>

<table>
  <thead>
   <!-- <tr>
      <th>نام</th>
      <th colspan="3"></th>
	 </tr> -->
  </thead>

  <tbody>
    <% @poets.each do |poet| %>
      <tr>
        <td><%= link_to poet.name, poet %></td>
				<td><%= link_to image_tag(poet.image_url(:thumb).to_s), poet %>
        <!--<td><%= link_to 'Show', poet %></td>
        <td><%= link_to 'Edit', edit_poet_path(poet) %></td>
				<td><%= link_to 'Destroy', poet, method: :delete, data: { confirm: 'Are you sure?' } %></td> -->
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Poet', new_poet_path %>
